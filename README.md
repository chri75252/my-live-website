# The Blacksmith Market

The public static website remains hosted by GitHub Pages. Cloudflare provides authoritative DNS, TLS, proxying, and the canonical apex-to-`www` redirect. The future Ask The Forge backend is isolated under `backend/forge-api`; no public form is enabled.

Migration and rollback records are in `docs/cloudflare-migration/`.
